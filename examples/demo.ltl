// examples/demo.ltl — runnable demo for ltl_connect4

import std::io
use ltl_connect4 as lib

fn main() {
    io::println("ltl_connect4 demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}