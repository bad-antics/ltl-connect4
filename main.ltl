// ltl-connect4/main.ltl
//
// Connect-Four with alpha-beta search + transposition table.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-connect4 v0.1.0")
    io::println("Connect-Four with alpha-beta search + transposition table.")
    0
}
